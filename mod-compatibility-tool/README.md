# Dark Mode Map CSS Patcher

Patches Dark Mode Map into other mods

## Usage

1. Extract patcher folder.
2. Drop the mod `.zip` in the folder, then run `patch.bat`.
3. If no zip is present, `patch.bat` will prompt you to drag one in.